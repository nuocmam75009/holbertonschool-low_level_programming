#include <stdio.h>

/**
 * main - print is name followed by a new line
 * @argc: argument count
 * @argv: array of arguments
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
